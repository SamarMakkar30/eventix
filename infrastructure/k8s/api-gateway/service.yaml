apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: eventix
  labels:
    app: api-gateway
    prometheus-scrape: "true"
spec:
  selector:
    app: api-gateway
  ports:
    - port: 8080
      name: http
      targetPort: 8080
      nodePort: 30080
  type: NodePort
