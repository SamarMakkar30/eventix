apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: eventix
  labels:
    app: booking-service
    prometheus-scrape: "true"
spec:
  selector:
    app: booking-service
  ports:
    - port: 8084
      name: http
      targetPort: 8084
  type: ClusterIP
