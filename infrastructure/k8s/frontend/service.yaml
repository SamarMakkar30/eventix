apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: eventix
spec:
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30300
  type: NodePort
