apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: eventix
  labels:
    app: inventory-service
    prometheus-scrape: "true"
spec:
  selector:
    app: inventory-service
  ports:
    - port: 8083
      name: http
      targetPort: 8083
  type: ClusterIP
