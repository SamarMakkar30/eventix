apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: eventix
  labels:
    app: notification-service
    prometheus-scrape: "true"
spec:
  selector:
    app: notification-service
  ports:
    - port: 8086
      name: http
      targetPort: 8086
  type: ClusterIP
